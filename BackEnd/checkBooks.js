const { PrismaClient } = require('./generated/prisma'); const prisma = new PrismaClient(); async function getBookDetails() { try { const books = await prisma.libros.findMany({ include: { ejemplares: true, libros_categorias: true, proveedores_libros: true, compras: true } }); console.log(JSON.stringify(books, null, 2)); } catch (error) { console.error(error); } finally { await prisma.(); } } getBookDetails();
